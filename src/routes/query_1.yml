---
summary: Custom Query
description: "Uma consulta SQL que retorna o nome, e-mail, a descrição do papel e as descrições das permissões/claims que um usuário possui."
tags:
  - API
produces:
  - application/json
parameters:
  - in: path
    name: user_id
    description: user id
    required: true
responses:
  200:
    description: SUCCESS
  400:
    description: BAD REQUEST
  404:
    description: NOT FOUND
  500:
    description: INTERNAL SERVER ERROR
